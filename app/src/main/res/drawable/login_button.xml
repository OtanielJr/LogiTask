<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Shadow Layer -->
    <item android:left="3dp" android:top="3dp" android:right="3dp" android:bottom="0dp">
        <shape android:shape="rectangle">
            <solid android:color="#40000000"/> <!-- Semi-transparent black for shadow effect -->
            <corners android:radius="12dp"/> <!-- Slightly larger radius for shadow -->
        </shape>
    </item>

    <!-- Button Shape Layer -->
    <item>
        <selector>
            <!-- Focused and enabled state -->
            <item android:state_enabled="true" android:state_focused="true">
                <shape android:shape="rectangle">
                    <solid android:color="@color/pumpkin"/>
                    <corners android:radius="10dp"/>
                    <stroke android:color="@color/bice_blue" android:width="2dp"/>
                </shape>
            </item>

            <!-- Enabled state -->
            <item android:state_enabled="true">
                <shape android:shape="rectangle">
                    <solid android:color="@color/pumpkin"/>
                    <corners android:radius="10dp"/>
                    <stroke android:color="@color/pumpkin" android:width="2dp"/>
                </shape>
            </item>
        </selector>
    </item>

</layer-list>
